<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LEGO Set Details</title>
    <link rel="stylesheet" href="/css/main.css">
</head>

<body class="min-h-screen bg-black">
    <%- include('partials/navbar',{page:"/lego/sets"}) %>

    <main class="container mx-auto px-4 py-8">
        <article class="max-w-4xl mx-auto border-4 border-red-600 bg-zinc-900">
            <header class="border-b-4 border-red-600 p-6">
                <h1 class="text-5xl font-mono font-black text-center tracking-tight text-white"><%= set.name %></h1>
            </header>
            
            <div class="p-6">
                <img src="<%= set.img_url %>" alt="<%= set.name %>" class="w-full border-4 border-red-600"/>
                
                <div class="grid grid-cols-1 md:grid-cols-3 gap-4 my-8 font-mono">
                    <div class="border-4 border-red-600 p-4 bg-zinc-800 hover:bg-zinc-700 transition-colors">
                        <div class="text-sm uppercase text-red-500">Year Released</div>
                        <div class="text-3xl font-bold text-white"><%= set.year %></div>
                    </div>
                    <div class="border-4 border-red-600 p-4 bg-zinc-800 hover:bg-zinc-700 transition-colors">
                        <div class="text-sm uppercase text-red-500">Theme</div>
                        <div class="text-3xl font-bold text-white"><%= set.theme %></div>
                    </div>
                    <div class="border-4 border-red-600 p-4 bg-zinc-800 hover:bg-zinc-700 transition-colors">
                        <div class="text-sm uppercase text-red-500">Parts</div>
                        <div class="text-3xl font-bold text-white"><%= set.num_parts %></div>
                    </div>
                </div>

                <blockquote class="border-4 border-red-600 p-4 my-8 bg-zinc-800" id="quote">
                    <p class="quote-content font-mono text-lg text-white">Loading quote...</p>
                    <p class="quote-author text-right mt-2 font-bold text-red-500">...</p>
                </blockquote>

                <div class="text-center">
                    <button onclick="history.back()" 
                            class="font-mono font-bold text-lg px-8 py-3 bg-red-600 text-white hover:bg-red-700 transition-colors border-4 border-red-600">
                        ← GO BACK
                    </button>
                </div>
            </div>
        </article>
    </main>

    <script>
        document.addEventListener("DOMContentLoaded", () => {
            fetch("http://api.quotable.io/random")
                .then(response => response.json())
                .then(data => {
                    document.querySelector(".quote-content").innerText = data.content;
                    document.querySelector(".quote-author").innerText = `— ${data.author}`;
                });
        });
    </script>
</body>
</html>